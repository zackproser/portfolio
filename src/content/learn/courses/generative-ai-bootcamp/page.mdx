import { CourseLayout } from '@/components/CourseLayout'
import { Button } from '@/components/Button'
import { createMetadata } from '@/utils/createMetadata'
import  GenAILandingPage from '@/components/GenAILandingPage'
import { Container } from '@/components/Container'

export const metadata = createMetadata({
  author: 'Zachary Proser',
  date: '2024-02-27',
  title: 'Generative AI Bootcamp', 
  description: 'Generative AI Bootcamp',
  image: 'https://zackproser.b-cdn.net/images/generative-ai-bootcamp.webp', 
})

export const status = 'available'

export default (props) => {
  return (<Container className="mt-16 lg:mt-32"><GenAILandingPage /></Container>);
}

