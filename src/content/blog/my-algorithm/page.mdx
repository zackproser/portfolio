import { createMetadata } from '@/utils/createMetadata'
import rawMetadata from './metadata.json'
import Image from 'next/image'
import SensorBotSceneWrapper from '@/components/SensorBotSceneWrapper'

export const metadata = createMetadata(rawMetadata)

The orange wire again.

<Image
  src="https://zackproser.b-cdn.net/images/intensity-and-i-wire.webp"
  alt="A figure rendered in pixel art with a glowing orange wire of gasoline and electricity threaded through heart, lungs, and brain"
  width={1200}
  height={675}
  className="rounded-lg my-8"
/>

Gasoline. Electricity. The wire that doesn&rsquo;t stop. Companion piece: [&ldquo;Intensity&rdquo; and I](/blog/intensity-and-i).

This is what it does.

It scans.

<SensorBotSceneWrapper variant="scan" showAudioToggle />

Do-do-do-do.

Left to right. Left to right.

The sensor is looking for something to chew.

It does not check the battery. It does not check the hour. Charge is a rumor.

---

Then it finds something.

The wire goes hot. The signal sharpens. The mouth opens.

<SensorBotSceneWrapper variant="chewing" />

Only here does the noise quiet.

Only here am I okay.

A phrase, repeating, the only thing the algorithm hears clean:

*I only exist in combat.*

The dopamine drip. The lock. The chunk. The rip. Fourteen hours pass without sensation. The body is irrelevant. The body is fuel.

---

Then it ends.

The target is gone. Consumed. Iterated through. Moved past. The wasteland clears.

The sensor keeps sweeping.

<SensorBotSceneWrapper variant="hill" />

This is where the suffering lives. The space between targets. The pause that does not pause.

The wire still runs warm. The sensor still sweeps. Nothing locks. Nothing to lock onto.

<Image
  src="https://zackproser.b-cdn.net/images/my-algorithm-closing.webp"
  alt="The sensor bot head alone on a hill at the edge of an empty wasteland, scanning into solitude"
  width={1200}
  height={675}
  className="rounded-lg my-8"
/>

A hungry ghost on a hill. The Buddhist word is *preta* &mdash; a being whose hunger has outlasted everything it was supposed to want.

The machine underneath this thing has a name. See: [the 14-hour hyperfocus pattern and what comes after](/blog/executive-function-as-a-service).

The robot does not know why it&rsquo;s still scanning.

It just is.
