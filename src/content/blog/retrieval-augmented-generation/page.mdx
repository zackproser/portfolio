import { Button } from '@/components/LinkButton'
import Image from 'next/image'
import { createMetadata } from '@/utils/createMetadata'
import rawMetadata from './metadata.json';

export const href = "https://www.pinecone.io/learn/retrieval-augmented-generation"

export const metadata = createMetadata(rawMetadata);

--- 

<Image src={metadata.image} alt="Retrieval Augmented Generation" width={800} height={600}/>

This was the first article I published while working at Pinecone: 

<Button href={href} variant="solid" color="green">Read article</Button>


