import { ArticleLayout } from '@/components/ArticleLayout'
import { Button } from '@/components/LinkButton'
import Image from 'next/image'
import PPP from '@/images/pain-poetry-python.webp'

import { createMetadata } from '@/utils/createMetadata'

export const href = "https://pinecone.io/blog/pain-poetry-python"

import rawMetadata from './metadata.json' assert { type: 'json' };
export const metadata = createMetadata(rawMetadata);

export default (props) => <ArticleLayout metadata={metadata} {...props} />

--- 

<Image src={metadata.image} alt="The Pain and Poetry of Python"/>

This was the fourth article I published while working at Pinecone: 

<Button href={href} variant="solid" color="green">Read article</Button>


