import { ArticleLayout } from '@/components/ArticleLayout'

import pineconeRefArchDeploy1 from '@/images/pinecone-ref-arch-deploy-part-1.webp'

import { createMetadata } from '@/utils/createMetadata'

export const metadata = createMetadata({
  author: "Zachary Proser",
  date: "2023-11-27",
  title: "Deploying the Pinecone AWS Reference Architecture - Part 1",
  description: "I deploy the Pinecone AWS Reference Architecture end to end in this three part video series",
  image: pineconeRefArchDeploy1
})

export default (props) => <ArticleLayout metadata={metadata} {...props} />

<iframe 
  className="youtube-video"
  src="https://www.youtube.com/embed/ySznARngHts"
  title="YouTube video player" 
  allow="fullscreen;">
</iframe>

In this three part video series, I deploy the Pinecone AWS Reference Architecture with Pulumi from start to finish.
