import { Button } from '@/components/LinkButton'
import Image from 'next/image'
import JSAI from '@/images/javascript-ai.webp'

import { createMetadata } from '@/utils/createMetadata'

export const href = "https://www.pinecone.io/learn/javascript-ai"

import rawMetadata from './metadata.json' assert { type: 'json' };
export const metadata = createMetadata(rawMetadata);

--- 

<Image src={metadata.image} alt="JavaScript for AI"/>

This was the second article I published while working at Pinecone: 

<Button href={href} variant="solid" color="green">Read article</Button>
